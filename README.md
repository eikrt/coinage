# Coinage

plotting application to predict when sold items used in a business should be bought

## Structure

total number of items and total items lost per action in input_data.csv. estimate.csv holds estimates of items sold per month.

